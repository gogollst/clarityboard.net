Ich habe ein Übergabepaket aus einer Claude.ai-Session im Ordner `handoff/`. 
Es enthält Referenz-Implementierungen und optimierte Prompts für die automatische 
Belegklassifizierung (Invoice Classification) via Anthropic Claude API.

Bitte arbeite das Paket wie folgt ein:

1. Lies ZUERST `handoff/HANDOFF_INSTRUCTIONS.md` — das ist dein Arbeitsauftrag mit 
   allen Regeln und Einschränkungen.

2. Lies DANN alle Dateien im `handoff/`-Ordner, um den Gesamtkontext zu verstehen.

3. Mache dich DANACH mit der bestehenden Projektstruktur vertraut, bevor du 
   irgendetwas änderst.

4. Arbeite die Änderungen in der Reihenfolge ein, die in HANDOFF_INSTRUCTIONS.md 
   definiert ist (Prompts → Models → Service-Logik → Tests).

WICHTIG: Die Dateien im handoff-Ordner sind REFERENZMATERIAL. Übernimm sie NICHT 1:1, 
sondern integriere die Inhalte in die bestehende Projektstruktur und -konventionen. 
Bei Unklarheiten frag mich, bevor du größere Änderungen machst.
